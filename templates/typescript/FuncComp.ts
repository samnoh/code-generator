import React, {useState, useEffect, useCallback} from 'react';
MODULES
type Props<T extends object> = {

};

const TEMPLATE_NAME = <T extends object>(props: Props<T>) => {
    return <></>;
};

export default TEMPLATE_NAME;